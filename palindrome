class Solution:
    def isPalindrome(self, s: str) -> bool:
        s=s.lower()
        s="".join(i for i in s if i.isalnum())
        rev=s[::-1]
        if s==rev:
            return True
        else:
            return False
